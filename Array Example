#include <iostream>
using namespace std;

int main()
{
  float numbers[5], sum = 0;
  cout << "Enter 5 numbers to be added together: \n";
  
  for (int x = 0; x < 5; ++x)
  {
    cin >> numbers[x];
    sum += numbers[x];
  }

  cout << "Sum = " << sum << endl;

  return 0;

}
