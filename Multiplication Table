#include <iostream>
using namespace std;

int main()
{
  int n, i, ni;
  cout << "Enter an integer: \n";
  cin >> n;

  for (i = 1; i <= 12; ++i)
    {
      ni = n * i;
      cout << n << " * " << i << " = " << ni << endl;
    }

  return 0;
}
